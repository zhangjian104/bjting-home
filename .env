# 公共基础路径
VITE_PUBLIC_PATH = /

# 路由模式: hash | history
VITE_ROUTER_MODE = history

# 接口地址（指向本地代理的路径）
VITE_APP_BASE_API = '/api'

# 核心接口的基础路径 (开发走本地代理)
VITE_API_BASE_URL=/sapi

# 本地所有 R2 资源统一的开发基础路径
VITE_RESOURCE_LOCAL_MODE=true
VITE_RESOURCE_BASE_URL=/r2/
