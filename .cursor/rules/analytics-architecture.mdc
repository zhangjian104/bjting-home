---
description: PostHog 埋点与指标口径（DAU/PV/收听书单）
globs: src/utils/analytics.ts,src/main.ts
alwaysApply: false
---

# Analytics 模块

## 关键文件

| 文件 | 职责 |
|------|------|
| `src/utils/analytics.ts` | `initAnalytics`、`trackPageView`、`trackAudiobookPlayStart`、`identifyClerkUser`、`resetClerkUser` |
| `src/composables/useClerkSync.ts` | Clerk 登录/登出时调用 PostHog identify / reset |
| `src/main.ts` | 应用启动时调用 `initAnalytics()` |

## 事件

- `$pageview`：路由切换后上报，承载 DAU / PV 统计
- `audiobook_play_start`：章节实际开始播放（`playing`）时上报

## 用户属性（匿名用户也可统计）

- `last_book_id`
- `listened_book_ids`
- `listened_book_titles`
- `listened_book_count`

以上属性通过 `posthog.register` 写入，并在本地 `localStorage(ph_listened_books)` 做去重维护。

## Clerk 登录用户标识

- 登录成功：`identifyClerkUser(user)` → `posthog.identify(user.id, { email, name })`
- 登出：`resetClerkUser()` → `posthog.reset()`
- 触发位置：`useClerkSync`（`App.vue` 挂载）

## 指标口径

- **日活（DAU）**：`$pageview` 的去重用户（按天）
- **访问量（PV）**：`$pageview` 总次数
- **每用户听过哪些书**：按 `distinct_id` 查看 `listened_book_ids` / `listened_book_titles` 或 `audiobook_play_start` 事件明细
