---
description: 项目上下文与有声小说改造说明
alwaysApply: true
---

# 项目上下文 (bjting-home Audiobook Platform)

这是一个正在从“音乐播放器”改造成“有声小说平台”的 Vue 3 前端项目。

## 核心业务目标

- 保持原有的毛玻璃（Glassmorphism）UI 设计和大部分交互功能。
- **词汇替换映射**：
    - 歌曲 -> 章节
    - 歌单 -> 书单
    - 专辑 -> 合集
    - 歌手 / 艺术家 -> 演播者 / 主播
    - 歌词 -> 文本（目前隐藏）
    - MV -> 视频（目前隐藏）
- **数据源替换**：逐步将底层依赖的网易云音乐 API 替换为本地的有声小说 Cloudflare Workers 后端接口。
- **API 替换**：
- 改动代码无需兼容网易云接口，直接替换为输入的接口模式。

## 基础后端接口约定 (开发环境)

- **API 封装规范**：所有针对 `/sapi` 的新请求必须统一封装在 `src/api/index.ts` 中（例如 `export const getPopularAuthors = () => fetch('/sapi/authors/popular').then(r => r.json());`），严禁在 `.vue` 组件中直接调用 `fetch` 或 `axios`。
- **API 接口**：统一走 `/sapi` 代理（映射到后端 `/api`）。本地实际指向 `http://127.0.0.1:8788`。
- **静态资源 (CDN)**：图片、音频等媒体资源已经全部托管在 R2 存储桶并绑定了自定义域名。前端不再通过 `?key=` 查询参数或者本地服务代理的形式请求，而是通过后端 API 返回的绝对路径（例如 `https://cdn.bjting.com/book/shao-fu-bai-jie/cover.jpg`）直接访问资源。在部分未提供绝对路径的场景中，前端会用相对路径通过 `https://cdn.bjting.com/` 进行拼接。

## AI 交互规则

1. 代码注释和与用户的日常对话必须使用**中文**。
2. 除非用户明确要求，否则不要生成冗长的、对代码无帮助的说明性 md 文档。

## 服务端与前端协同开发
